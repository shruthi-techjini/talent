{% extends 'base.html.twig' %}

{% block body %}
	<div class="panel panel-default">
		<div class="panel-heading">
			<h2 class="center">List of all genres</h2>
			<a class="btn btn-success pull-right" href="{{ path('languages_new') }}" style="margin-top:-45px;">Create a new language</a>
		</div>
		
		<div class="panel-body table-responsive">
			<table class="table table-hover">
    			<thead>
    				<tr>
                        <th>Id</th>
                        <th>Name</th>
                        <th>Actions</th>
                    </tr>
    			</thead>
    			<tbody>
    				 {% for category in languages %}
                        <tr>
                            <td>{{ category.id }}</td>
                            <td>{{ category.name }}</td>
                            <td>
{#                             	<a href="{{ path('category_show', { 'id': category.id }) }}">#}
{#                             		<i class="glyphicon glyphicon-eye-open" aria-hidden="true"> show</i>#}
{#                             	</a>#}
{#                             	<br/>#}
                            	<a href="{{ path('languages_edit', { 'id': category.id }) }}">
                            		<i class="glyphicon glyphicon-pencil" aria-hidden="true"> edit</i>
                            	</a>
                            </td>
                        </tr>
                    {% endfor %}
    			</tbody>
		</div>
	</div>
{% endblock %}





