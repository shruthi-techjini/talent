{% extends 'base.html.twig' %}

{% block body %}
	<div class="panel panel-default">
		<div class="panel-heading">
			<h2 class="center">List of all genres</h2>
			<a class="btn btn-success pull-right" href="{{ path('genre_new') }}" style="margin-top:-45px;">Create a new genre</a>
		</div>
		
		<div class="panel-body table-responsive">
			<table class="table table-hover">
    			<thead>
    				<tr>
                        <th>Id</th>
                        <th>Name</th>
                        <th>Status</th>
                        <th>Actions</th>
                    </tr>
    			</thead>
    			<tbody>
    				 {% for category in genres %}
                        <tr>
                            <td>{{ category.id }}</td>
                            <td>{{ category.name }}</td>
                            <td>{{ category.status | status }}</td>
                            <td>
{#                             	<a href="{{ path('category_show', { 'id': category.id }) }}">#}
{#                             		<i class="glyphicon glyphicon-eye-open" aria-hidden="true"> show</i>#}
{#                             	</a>#}
{#                             	<br/>#}
                            	<a href="{{ path('genre_edit', { 'id': category.id }) }}">
                            		<i class="glyphicon glyphicon-pencil" aria-hidden="true"> edit</i>
                            	</a>
                            </td>
                        </tr>
                    {% endfor %}
    			</tbody>
		</div>
	</div>
{% endblock %}





